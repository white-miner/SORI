# AGENTS.md — sori

이 파일은 이 저장소에서 작업하는 모든 AI 코딩 에이전트(Cursor, Codex, Claude Code 등)가
**작업 시작 전에 반드시 읽어야 하는 계약서**다. 사람용 README가 아니라 에이전트용 운영 규칙이다.

---

## 0. 이 저장소의 가장 중요한 규칙

**기존에 동작하는 로직을 깨뜨리는 것은, 새 기능을 못 만드는 것보다 훨씬 나쁘다.**

sori는 이미 운영 중인 상담차트 · 고객차트 흐름을 가지고 있다.
새 설계(Visit 중심 3단 구조)는 그 흐름을 **대체**하는 것이 아니라 **확장**하는 것이다.

무언가 애매하면: 삭제하지 말고, 이름을 바꾸지 말고, 시그니처를 바꾸지 말고 — **멈추고 물어봐라.**

---

## 1. 프로젝트 개요

- 제품: `sori` — 1인 샵(에스테틱 · 케어 · 타투) 사장님용 고객관리 · 전후사진 앱
- 철학: 모든 기록은 리뷰이며 소통으로 완성된다
- 핵심 도메인 3축
  1. **상담차트** — 최초 상담 · 니즈 · 동의 · 목표 설정
  2. **고객차트** — 고객 1명의 영구 기록 컨테이너
  3. **방문(Visit)** — 그날의 이용서비스 · 결제 · 사진 · 상태기록을 묶는 트랜잭션 단위

### 데이터 계층 (이 위계를 어기는 코드는 리뷰에서 거부된다)

```
Customer
 ├─ Consultation   (상담차트 — 고객당 1..N, 보통 최초 1건 + 리컨설팅)
 └─ Visit          (방문 1회 = 모든 방문 기록의 부모)
      ├─ ServiceItem[]
      ├─ Payment[]        ← 현금 유입
      ├─ CreditLedger[]   ← 선불권 권리 소모 (Payment와 절대 합치지 않는다)
      ├─ ChartNote        ← SOAP 4필드
      └─ Photo[]          ← PhotoSet에 소속, Visit을 참조

PhotoSet (Customer 소속, 부위/각도 단위) ─┬─ Photo
                                          └─ pose_template
```

**불변 규칙**
- 사진은 `PhotoSet`(부위·각도)에 소속되고 `visit_id`로 방문을 **참조**한다. Visit에 직접 매달지 않는다.
- `Payment`와 `CreditLedger`는 별개 테이블이다. 매출 집계는 Payment만, 부채 집계는 CreditLedger만 본다.
- 상담차트는 방문 기록이 아니다. Visit로 흡수하려 하지 마라.

---

## 2. 에이전트 작업 프로토콜 (반드시 이 순서)

### STEP 1 — 읽기 전용 조사
코드를 한 줄도 고치기 전에:
1. 관련 파일을 모두 읽고, **현재 동작을 3~10줄로 요약**해서 먼저 출력한다.
2. 이번 변경이 건드리는 **호출부 전체 목록**(grep 결과)을 출력한다.
3. `docs/CONTRACTS.md`에 등재된 보호 대상 계약을 건드리는지 확인한다.

### STEP 2 — 계획 제출 후 승인 대기
다음 형식으로 계획을 먼저 낸다. **승인 없이 파일을 수정하지 않는다.**

```
## 변경 목적
## 건드리는 파일 (전체 목록)
## 건드리지 않는 것 (명시)
## 기존 동작 중 바뀌는 것 / 안 바뀌는 것
## 롤백 방법
## 검증 방법 (실행할 테스트 명령)
```

### STEP 3 — 특성화 테스트 먼저
변경 대상에 테스트가 없다면, **고칠 코드의 "현재 동작"을 그대로 박제하는 테스트를 먼저 작성**한다.
이 테스트는 "옳은 동작"이 아니라 "지금 동작"을 기록한다. 버그까지 포함해서.
이걸 통과시킨 다음에야 리팩터링을 시작한다.
([characterization test](https://getunblocked.com/blog/refactoring-legacy-code/) 방식)

### STEP 4 — 한 번에 하나
- 커밋 1개 = 논리적 변경 1개. 리팩터링과 기능 추가를 같은 커밋에 섞지 않는다.
- 한 번의 작업 지시에서 수정 파일 **5개 초과 시 중단하고 분할 제안**.
- 요청받지 않은 "겸사겸사 개선"(포맷팅, 리네이밍, 의존성 업그레이드, 폴더 재배치) 금지.

### STEP 5 — 검증 후 보고
`npm run typecheck && npm run test && npm run build` (또는 프로젝트 실제 명령)을
**실제로 실행하고 출력을 붙여서** 보고한다. "통과할 것으로 예상됨"은 보고가 아니다.

---

## 3. 절대 금지 (승인 없이는 예외 없음)

- 기존 테이블/컬럼의 **삭제 · 이름 변경 · 타입 변경**
- 기존 API 라우트의 **삭제 · 경로 변경 · 응답 필드 제거**
- 기존 함수/컴포넌트의 **삭제 · 이름 변경 · 인자 순서 변경**
- 마이그레이션 파일의 **수정 또는 되돌리기** (항상 새 마이그레이션을 추가한다)
- `git reset --hard`, `git push --force`, 브랜치 삭제
- 라이브러리 메이저 버전 업그레이드
- 동작하는 코드를 "더 깔끔해서" 재작성 — 요청받은 적 없으면 하지 않는다
- 상담차트 ↔ 고객차트 연결 로직을 **한 번에 통째로 갈아엎기**

---

## 4. 스키마 변경은 무조건 Expand → Backfill → Contract

파괴적 스키마 변경을 한 번에 하지 않는다. 3단계로 쪼갠다
([expand-contract 패턴](https://www.prisma.io/dataguide/types/relational/expand-and-contract-pattern)).

1. **Expand** — 새 테이블/컬럼을 **추가만** 한다. 전부 nullable. 기존 코드는 그대로 돈다.
2. **Backfill** — 기존 데이터를 새 구조로 복사하는 스크립트를 돌린다. 읽기는 이중화(신규 우선, 없으면 구형 폴백).
3. **Contract** — 신규 경로가 최소 2주 무사고로 돌아간 뒤에야 구형 컬럼을 제거한다.

각 단계는 **별도 PR, 별도 배포**다. 한 PR에 두 단계를 넣지 않는다.

---

## 5. 명령어

```bash
npm run dev         # 개발 서버
npm run typecheck   # 타입 검사 — 모든 변경 후 필수
npm run test        # 단위 + 특성화 테스트 — 모든 변경 후 필수
npm run test:e2e    # 핵심 플로우 회귀 테스트
npm run build       # 빌드 검증
```

> 실제 명령이 다르면 이 섹션을 사실에 맞게 고쳐서 쓸 것. 여기 적힌 명령이 곧 에이전트의 검증 기준이 된다.

---

## 6. 보호 대상 플로우 (깨지면 즉시 롤백)

아래는 사용자가 매일 쓰는 경로다. 어떤 변경이든 이 6개가 끝까지 동작해야 한다.

1. 신규 고객 등록 → 상담차트 작성 → 저장
2. 상담차트 → 첫 방문 생성 → 기준 사진 촬영
3. 기존 고객 검색 → 고객차트 열기 → 방문 타임라인 확인
4. 방문 기록 생성 → 서비스 선택 → 촬영 → 결제 → SOAP → 저장
5. 선불권 결제 → 다음 방문에서 차감 → 잔여 횟수 정확히 감소
6. PhotoSet 선택 → 전/후 슬라이더 비교

이 목록은 `npm run test:e2e`의 시나리오와 1:1로 대응해야 한다.
